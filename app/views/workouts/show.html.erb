<p id="notice"><%= notice %></p>

<div class="row">
  <aside class="col-md-4">
    <section>
      <% if @workout.weekday? %>
        <h1><%= @workout.weekday %></h1>
      <% end %>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @workout.workout_log? %>
      <h3>Log Notes: <%= @workout.workout_log %></h3>
    <% end %>
  </div>
</div>

<%
=begin %>
<%= link_to 'Edit', edit_workout_path(@workout) %> |
<%
=end %>
<%= link_to 'Back', workouts_path %>
